# 全网事件回放：已实现范围与解释边界

本文件和 `event_replay.py` 只属于本次新实验目录。旧数据、旧实验及预测器不改动。

## 当前可运行的部分

已经找到并核对以下输入：

- `D:/physic_predict_bike/divvy_2017_physics_closed_active_v7_10min_from_v64/accepted_july_trips_v7.csv.gz`：原 V7 接纳的历史成功骑行，含借还时刻及起终站；保留原来剔除的 33 条同车重叠记录，不再选择性筛选。
- `data/DIVVY/DIVVY.npz`：567 实体站加用户在途节点的整数状态。2017-07-27 00:00 为 5274 辆站内、36 辆在途，共 5310 辆；当时未完成的 36 笔骑行可与在途数逐项对账。
- `paper_alns_v3/results/station_capacities.csv`：与既有大规模实验一致的站容。它是全月重构库存最大值，属于事后容量估计，不能叫作实测车位数。原 V7 重构并未强制实测车位约束。

主验证是 **567 站、2017-07-27 整日**，不制作任何小规模实例。它是运行事件引擎和检查车数守恒的数据验证，没有新增一个优化比较组，也不从中作 B/C 优劣结论。

运行：`D:/anaconda3/python.exe -B event_replay.py`。结果写入本目录 `results/event_validation`，运行前保存规则与输入哈希，结束后保存十分钟状态和校验结果。

整日验证已完成：18,241 笔起点之后的观察借车请求、35,986 次事件/边界物理校验，最大车队守恒误差为 **0 辆**。终点状态为 5,214 辆站内、42 辆骑行、54 辆在目的站外等候归还，合计仍为 5,310 辆。635 次失败借车、264 次首次失败归还只是这个无派车引擎验证的计数；其中 210 次失败归还已在后续空位出现时成功入站。它们不构成模型比较或对实际潜在需求损失的估计。

## 物理状态与事件规则

全程逐事件核对：

\[
\sum_i I_i+I_{\rm riding}+I_{\rm waiting}+\sum_k L_k=5310.
\]

`riding` 与 `waiting` 分开：满站未能归还的车没有消失，也不能继续解释为正在骑行。

1. 借车成功才减少站内库存，并生成该请求的后续还车；失败借车不会在原定时刻凭空生成一辆归还车。
2. 首次归还没有空位时计一次失败。当前预先固定的处理是顾客在目的站外排队，车仍属系统外部库存；该站出现空位时按 FIFO 归还。没有免费跨站传送、没有删除单车、没有无限重复计同一还车失败。此规则是假设，尚不是从 Divvy 观测辨识出来的实际行为。
3. 同一时刻先处理归还，再处理卡车事件，最后处理借车；每类事件以固定顺序执行。源数据是分钟级时标，不能把它描述成真实秒级观测。
4. V7 初始边界已包含恰好发生在该时刻的事件，因此回放时间为 `(2017-07-27 00:00, 2017-07-28 00:00]`。起点的两次借车不重复执行；跨起点在途骑行继续完成。
5. 起点之后不再读取历史库存覆盖模拟库存，**不执行任何 V7 推断运营重定位**。否则会把历史推断调运和新策略同时加入。
6. 单车可交换使用；历史 `bike_id` 不被强制作为顾客要借的具体车。每笔成功骑行仅提供一个观察到的起终站和时间请求。

## 已预留卡车执行接口

`ObservedRequestReplay.dispatch_route(truck_index, route, targets)` 接收一条当前下发的站点路线和当轮固定整数目标。距离矩阵顺序明确为 567 实体站，最后一行为仓库。

- 默认 4 车、容量 30；初始仓库空载。站点当前状态和车载量决定到站实际装卸，不能读取未来库存。
- 行驶、停车及逐辆装卸有真实持续时间；`advance` 跨越一个十分钟边界时，在途路线和车载量全部保留。
- 有任务的卡车不能被瞬间重置或改派到另一位置。返回仓库仍携带的车留在车上，仓库没有免费补车或卸车功能。
- 到站按当前库存向固定目标作物理可行装卸，是一个因果执行规则，**不是静态两阶段模型里全情景已知后的最优追索**。未来若各方法接入，必须共用这个规则并明确此区别。
- 18 km/h、停车 15 秒、每辆处理 6 秒只是继承已有参数的可复现假设，不是从真实卡车轨迹估计得到。默认整日验证不派车，不使用这些参数来声称运营改善。
- 目前验证覆盖顾客事件和全网守恒。卡车接口还没有与四组优化器整日联调，因此不能称完整车辆滚动策略已验证。

## 不应接受的结论

历史数据只有成功骑行。输出名称是 **未服务的历史观察请求**（unserved observed requests），不能无条件叫作真实潜在需求损失。重构初始库存和容量也继承事后信息；无调度验证不是历史轨迹基线。

四组静态快照仍分别在共同实库存下评价目标偏差和距离。这个事件引擎不把已有 144 个独立快照拼贴成闭环，也不把每十分钟几十公里的路线瞬时执行。

尤其不能把现有固定预测导出直接叫作“PDST 根据新策略库存重新预测”。真正滚动闭环还需要：

1. PDST 从各策略当前仿真状态和可观测历史图重新推理，修正已知前瞻输入问题。
2. 允许卡车在任意当前位置和非零载量继续规划，保持已执行路径不可撤销。
3. 十分钟决策间隔下在期限内得到解，固定终止规则与共享执行参数。

本轮不修改预测器，不伪造这些条件已经满足。事件引擎是后续运营验证的可检查基础；当前 B/C 大规模结果仍应按回顾性静态机制实验报告。
