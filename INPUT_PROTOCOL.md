# 本轮144个大规模实例与误差集合

本轮数据已构造完成，入口为 `inputs/index.json`，每个实例包含567个实体站。只使用原先冻结的2017年7月27日144个时点（sample 176—319）。没有新增小规模实例，没有读取本轮测试实际库存进行划区、选择目标、拟合误差界或调参。原文件SHA-256在构造前后完全一致。

## 0节点物理边界修订：在读取结果前纠正模型遗漏

首批求解期间、实际库存仍未打开时，发现统计全网误差带没有与0节点非负库存相交。例如sample176名义实体库存总量为5280，旧统计误差带上界151.5会允许实体库存达到5431.5，从而在总车队5310下产生负的用户在途库存。旧144例中108例存在这一支持集合问题，最大允许超额549.2辆。首批算例与代码已保留在 `superseded_missing_node0_bound`，不能作为修正模型的论文结果。

车队总量F从已有 `data/DIVVY/dataset_meta.json` 的 `fleet_size` 读取，值为5310；同时核对原冻结预测归档的全部144条“0节点＋实体站”预测总量均等于F。核验仅打开prediction和sample_id，未打开actual。来源、哈希和核验记录已写入新输入协议。

在共用名义中心p′下，完整账目要求r₀=F−Σ_i(p′_i+ε′_i)∈[0,F]，故C₀和C₀V共同追加：

\[
-\sum_i p'_i\le\sum_i\epsilon'_i\le F-\sum_i p'_i.
\]

最终H₀是原统计误差合计带与上述物理界的交集。B仍作为**不含全网耦合的盒松弛模型**，并不声称其每一个联合误差组合都满足总车队物理账；D的名义实体总库存另行核验不超过F。B与C在真实库存回放时都使用相同物理车队，不允许实际制造自行车。

本项修订没有看运营效果选参数、改变原半径或重新划区。修正后的144例重新检查嵌套、非空和真正的单站投影范围，三鲁棒组的单站投影仍完全相同。当前0节点物理交集与历史统计覆盖分开报告：`statistical_global_band`保留原带，`physical_node0_global_band`保存物理界，`global_band`是求解使用的交集。不能因当前库存状态的物理交集排除了某些旧残差，就误称原统计校准失败。

## 固定分区与可选访问

区域由固定站点坐标递归中位数切分两次得到，先选择经纬度投影后跨度最长的轴；经度乘平均纬度的余弦，使尺度可比，坐标相同时按原站序破除并列。区域大小为141、142、142、142。区界和全部站号已保存于 `inputs/regions.json`。该规则不使用预测余缺、测试误差或运营成本，不会按结果重新划区。

区域用于同一联合误差集合与区域协同路径搜索，不是四个封闭小网络。所有567站均可选访问；原先的A/B分类通过 `original_candidate_mask` 保留，不能把原B类站当成免费供车点。目标库存、站点容量、路网距离、每个实例原有的共同距离上限不变。访问权限的开放对B/C₀/C₀V/D完全相同。

## 联合校准，保持真正相同的单站边界

直接读取每个旧实例保存的同步历史残差及时间。最早残差为2017年7月19日14:20，最晚为7月25日17:50，均早于实验起点。沿用相同时钟前后60分钟及相同工作日类型匹配；最后两个匹配日期作校准，更早日期拟合。每例有26—39条拟合残差、14—26条校准残差。

对单站残差、全网有符号残差之和、四个区域有符号残差之和分别拟合中心c与尺度s：

\[
c=(\min e+\max e)/2,\qquad s=\max\{1,(\max e-\min e)/2\}.
\]

每条校准残差的分数取上述所有标准化绝对偏差的最大值；共同半径ρ取全部校准分数最大值与1的较大者。**B、C₀、C₀V均使用这一个ρ以及完全相同的单站物理误差盒**。不能仅为C₀V扩大半径，也不能保持旧半径却隐瞒新增区域约束降低联合校准覆盖。

相比只包含单站和全网分数的旧半径，新增区域分数仅使sample211（05:50）由4增至4.5714285714，其余143例不变。这个扩大同样作用于该例的B与C₀。半径范围为3—11.8，说明高维联合覆盖确实较保守；本轮不为追求守恒收益另行缩小。

三个鲁棒集合严格嵌套：

\[
U_{C_0V}=U_B\cap H_0\cap\bigcap_g H_g\subseteq U_{C_0}=U_B\cap H_0\subseteq U_B.
\]

H_g是历史同步区域残差的有符号合计区间，既不是各区误差和为零，也不是独立求四区最坏损失再相加。区域虚站点是该区外部补集的记账：原始误差坐标下 ε_Vg=−Σ_{i∈g}ε_i。各虚站点互有重叠，不能再加总为实物库存，更不能作为卡车免费取送的站。

程序对全部144例逐一检查集合非空，保存每个集合的可行误差向量，并利用分区互不重叠的结构计算各集合**真正投影到每一站的最小/最大误差**。结果是：三个鲁棒集合在全部144例中的567个单站投影范围完全一致。因而本轮C的变化确实是联合误差关系的变化，没有暗中缩小某些站的有效边界。

全部鲁棒集合在做当前物理裁剪之前，对匹配校准残差达到100%经验联合覆盖。这只是有限历史样本的事实，不能声称未来100%或95%概率保证。将历史残差平移到当前预测并进行当前站容裁剪，是另外一项支持检查，不能误读为历史库存本身的可行性验证。

## 名义预测的共同物理处理

原始预测只有sample314（23:00）的一站超过原冻结站容。对四组统一设p′=clip(p,0,C)，原始p保存在 `predicted_original`，s=p−p′保存在 `center_shift`。目标库存不改；名义需求更新为e−p′。

鲁棒误差同步平移为ε′=ε+s：单站边界加s_i，全网合计带加Σs_i，区域合计带加Σ_{i∈g}s_i。于是

\[
p+\epsilon=p'+\epsilon'
\]

在同一校准半径下，鲁棒模型允许的**物理库存支持完全不变**。D使用共同的物理名义库存p′，而不是只有D临时换数据。由于原总量闭合于p，平移后的0节点关系相应带有已知总平移项；不能把ε′直接当作未经平移的原始PDST残差。

NPZ中的 `calibration_errors`、`historical_error_lower/upper` 均保留原始预测坐标，供溯源；求解应读取 `uncertainty_specs.json` 中已经平移的界。它包含B、C0、C0V和D四项；D的误差上下界均为零。

## 解释范围

本轮是追加回顾性快照实验，继承如下局限：数据以前已进入覆盖诊断；导出预测含既有动态图前瞻问题；库存从成功骑行回溯重构；站容使用全期事后估计；校准残差与checkpoint选择共用验证资料。十分钟是预测步长，不是整条路线的执行时限。当前输出中的目标库存偏差不能称为真实失单、潜在未满足需求或连续闭环运营结果。

复现：`D:/anaconda3/envs/pytorch_gpu39/python.exe -B prepare_inputs.py`。程序只写当前新实验目录，保留源文件哈希及输出哈希；遇到不兼容输入或空集合时记录失败并停止，不删除失败时点。
